# Phase 2 decisions

Every place the admin code built during Phase 1 disagrees with
`paelon-website-spec.md`, and every Phase 2 choice the spec leaves open. Drafted
2026-09-15 from spec §1, §8, §9, §10, §14, `docs/decisions.md`, and the code in
`app/(admin)/`, `components/admin/`, `lib/auth/`, `lib/uploadthing/`. Line
references are from commit `8847e46`.

**Status: decided 2026-09-15.** Every row has a Decision, and each is folded
into `paelon-website-spec.md` and listed under its "Amendments since 1.0". The
spec is the source of truth from here; this file keeps the reasoning and the
options that were rejected. It is the Phase 2 counterpart to
`docs/decisions.md`, which stays the Phase 1 record. A decision that changes
later is amended in the spec and noted in the row, not deleted.

B1 and B3 were taken as defaults rather than rulings and are awaiting
Francis's confirmation (CLAUDE.md, Blocked on Francis).

Recommendations are the drafter's view; the Decision column is what stands.

---

## P. Precondition

| Topic | Spec says (§ ref) | Code does (file:line) | Options | Recommendation | Decision |
|---|---|---|---|---|---|
| **P1 Whether the built panel stays** | §1: "must not build Phase 2 features during Phase 1". §3 marks `(admin)/`, `components/admin/`, `lib/auth/` "Phase 2. Do not build in Phase 1". §5: `users`/`sessions`/`accounts` "Do not implement in Phase 1" | About 2,430 lines across `app/(admin)/`, `components/admin/`, `lib/auth/`, `app/api/auth/`. CLAUDE.md still says ~1,375; staff management (`8847e46`) was added after that count. Live surfaces: `/admin/login`, `/admin/media`, `/admin/users`. `docs/decisions.md:58` still lists auth and admin as "deliberately not built" | **A** Keep it and ratify it as an early Phase 2 start, with the hours counted against the Phase 2 week.<br>**B** Ship it disabled: an env flag 404s `/admin/*` and `/api/auth/*` in production. The upload route stays closed because it requires a session (`lib/uploadthing/core.ts:67-71`). Editorial uploads keep working through `npm run upload`, which calls UTApi directly (`scripts/upload-asset.mjs:87`).<br>**C** Revert it. | **B.** The panel does media and staff management only, with no content CRUD. Its login has no IP rate limit (A1). Production would carry the attack surface without the payoff. Re-enable at Phase 2 kickoff once A1–A4 are decided. Whichever option is chosen, correct `docs/decisions.md:58` and the CLAUDE.md line count. | **A** — ratified as the Phase 2 base (2026-09-15). Overrides the drafted recommendation. |

---

## A. Authentication

| Topic | Spec says (§ ref) | Code does (file:line) | Options | Recommendation | Decision |
|---|---|---|---|---|---|
| **A1 Login IP rate limit** | §14 Auth: "Rate-limit login endpoint (5 attempts per 15 minutes per IP)" | None. `RATE_LIMITS` has no login rule (`lib/rate-limit/index.ts:32-39`), and `authorize()` never calls `checkRateLimit` (`lib/auth/config.ts:107`). Credentials are accepted through `loginAction` (`lib/auth/actions.ts:56`) and directly at `/api/auth/callback/credentials` (`app/api/auth/[...nextauth]/route.ts:13`). Every unknown-email attempt also writes an `audit_log` row (`lib/auth/config.ts:117`), so with no limit the table can be flooded | **A** Add `login: { limit: 5, windowSeconds: 900 }` and check it inside `authorize()`, which covers both entry points.<br>**B** Check it in `loginAction` only. This misses direct POSTs to the callback. | **A.** Reuse the existing salted-IP-hash limiter and return the same generic failure as every other login error. | **A** — accepted (2026-09-15). |
| **A2 Account lockout rule** | §14 Auth: "Lock account after 10 failed attempts pending admin unlock" | 5 consecutive failures lock the account for 15 minutes, then it unlocks on its own (`lib/db/queries/users.ts:16`, `:19`, `:65-80`). A super admin can clear it early (`lib/auth/user-actions.ts:246-266`). The login error tells anyone the rule (`lib/auth/actions.ts:66`), and the password guard's reasoning leans on "the five-attempt lockout" (`lib/auth/staff-guards.ts:92`) | **A** Spec as written: 10 failures, admin unlock only. Anyone who knows a staff email can lock that account indefinitely. If the last super admin is locked, nobody is left who can unlock them, and recovery means SQL against production.<br>**B** Code as written: 5 failures, auto-expires after 15 minutes.<br>**C** Hybrid: 10 failures trigger a 15-minute lock that auto-expires, and a super admin can clear it early. | **C**, with §14 amended. Once A1 exists, the per-IP limit stops online guessing. The per-account lock only matters for distributed attempts, where an indefinite lock becomes a lever for locking staff out. Whatever is chosen, the message at `actions.ts:66` must match it or stop quoting numbers. | **C** — accepted, §14 amended (2026-09-15). |
| **A3 JWT vs Postgres sessions** | §2 and §8 Access: "Sessions stored in Postgres" | `strategy: "jwt"` with an 8-hour `maxAge` (`lib/auth/config.ts:87`, `:68`). The role is re-read from the DB at most every 5 minutes (`:65`, `:186-207`). The `sessions` table is defined but never written (`lib/db/schema.ts:481-487`). `@auth/drizzle-adapter` is installed and imported nowhere (`package.json:32`). The stated reason is that Credentials does not issue DB sessions (`config.ts:21-31`). `getUserById`'s JSDoc says "on every request" (`users.ts:42`), which is wrong | **A** JWT plus the periodic re-check, as built. Revocation takes up to 5 minutes, and there is no "sign out everywhere".<br>**B** A JWT carrying an opaque session id, checked against `sessions` on each admin request. Revocation is instant and active sessions can be listed, at the cost of one query per request. Uses the existing table.<br>**C** The database strategy through the adapter with custom encode/decode. This works against Auth.js's intended Credentials behaviour and is fragile across v5 beta upgrades. | **A.** Amend the §2/§8 wording and remove the unused adapter. Move to **B** only if Paelon's incident process needs a revoked account out immediately. | **A** — accepted, §2/§8 amended, adapter to be removed (2026-09-15). |
| **A4 Password rules** | §8 Access: "min 12 chars, complexity checks via Zod" | Length 12–1024, and the password must not contain the email's local part. No composition rules, by design (`lib/auth/staff-guards.ts:87-116`). It is a plain function; the Zod schemas only require `min(1)` (`lib/auth/user-actions.ts:56`, `:63`). There is no forced change after an admin-set password | **A** Amend the spec to a length-based rule (NIST SP 800-63B) and express the rule as a Zod refinement, so "Zod at every boundary" holds.<br>**B** Add composition rules as the spec says. | **A.** Composition rules push people towards `Password1!`. The guard is already tested (`tests/staff-guards.test.ts`); add accept and reject cases for the Zod form. | **A** — accepted (2026-09-15). |
| **A5 How admin routes are protected** | §8 Access: "All admin routes protected by middleware" | `middleware.ts` only sets headers (`middleware.ts:7-9`, `:75-106`). The real checks are `requireAdminUser()` in the dashboard layout (`app/(admin)/admin/(dashboard)/layout.tsx:43`) and `requireCan()` in every action and route (`lib/auth/session.ts:76-83`, `lib/uploadthing/actions.ts:51`, `lib/uploadthing/core.ts:67-71`). The reasoning is at `lib/auth/config.ts:36-39`. The layout cannot keep `?next=` (`(dashboard)/layout.tsx:11-17`) | **A** Amend the spec: enforcement lives in layouts, actions and route handlers.<br>**B** A plus a middleware redirect for `/admin/*` when there is no session cookie. This is defence in depth only, since the edge cannot check role or lock state. It also restores `?next=`.<br>**C** Middleware only. Rejected: middleware cannot read the DB. | **B.** Server-side checks remain the control; the middleware redirect is cheap and fixes the lost return path. | **B** — accepted (2026-09-15). |
| **A6 First super admin and password recovery** | Silent | The only way to create the first account is SQL. The login page points to `npm run admin:passwd` (`app/(admin)/admin/login/page.tsx:15`), which does not exist (`package.json:8-30`). Resets are passwords set by a super admin and handed over in person (`lib/auth/user-actions.ts:268-310`). There is no self-service reset because no email provider is available | **A** CLI scripts `admin:create` and `admin:passwd`, run with `--env-file` against the target DB (a `package.json` change).<br>**B** Bootstrap an admin from env vars during seeding. This puts a password in env and CI.<br>**C** A manual SQL runbook.<br>Separately: a `must_change_password` column (a one-way migration) to force a change after any admin-set password. | **A**, plus fix the login page comment. Also add `must_change_password`, since every password is currently passed by hand. | **A** plus `must_change_password` — accepted (2026-09-15). |
| **A7 Packages outside §2** | §2 install list. §16: "Never install a package not on the approved list without asking" | `@node-rs/argon2` (`package.json:35`); the schema comment still says "Library not yet approved" (`lib/db/schema.ts:464`). `@auth/drizzle-adapter` (`package.json:32`) is unused. Neither is in the approvals row at `docs/decisions.md:24` | **A** Ratify both.<br>**B** Ratify argon2 and remove the adapter.<br>**C** Swap the argon2 binding. | **B.** §14 requires argon2id, so some binding is needed. This one ships prebuilt binaries with no node-gyp step, which suits self-hosting. Keep the adapter only if A3-B/C is chosen. | **B** — accepted (2026-09-15). |
| **A8 Auth and upload env vars** | §14 Env vars: `AUTH_SECRET`, `AUTH_URL`, `UPLOADTHING_SECRET`, `UPLOADTHING_APP_ID` | UploadThing v7's single `UPLOADTHING_TOKEN` replaces the pair (`lib/env.ts:52`). `UPLOADTHING_UPLOADS_ENABLED` is declared but read by nothing (`lib/env.ts:62`, `.env.example:35-38`). `AUTH_SECRET`/`AUTH_URL` are optional and commented "not consumed in Phase 1" (`lib/env.ts:37-39`, `.env.example:25`), yet `lib/auth/config.ts` consumes them. `trustHost: true` (`lib/auth/config.ts:89`) | Amend §14 to `UPLOADTHING_TOKEN` · delete the dead var · require `AUTH_SECRET` whenever admin is enabled (a `superRefine`, as for Resend) · keep `trustHost` for self-hosting behind a proxy, but require `AUTH_URL` in production | **All four.** Without `AUTH_URL`, `trustHost` builds callback URLs from the request's host headers. | All four — accepted (2026-09-15). |

---

## B. Roles and permissions

| Topic | Spec says (§ ref) | Code does (file:line) | Options | Recommendation | Decision |
|---|---|---|---|---|---|
| **B1 Role names in the UI** | §8 Roles: `admin`, `editor`, `contributor`. §8 Booking workflow: assignment goes to "users with `editor` or `admin` role" | The enum is unchanged (`lib/db/schema.ts:112-116`), but the UI labels `admin` as "Super admin", `editor` as "Admin" and `contributor` as "Media" (`lib/auth/policy.ts:176-180`; mapping at `:14-24`) | **A** UI labels match the enum names, as in the spec.<br>**B** Keep the team's labels and add the mapping to §8. | **B only if Paelon asked for these names; otherwise A.** "Admin" meaning `editor` makes every later spec line that says "admin" ambiguous. | **A** — UI labels match the enum. No request from Paelon for other names is on record (2026-09-15). |
| **B2 What editors can do** | §8 Roles: `editor` "CRUD on content, no user management". `admin` "user management". §8 routes: `/admin/users` "admin only" | Editors can create, update and publish all content, and can read patient submissions. They cannot delete services, doctors, locations, testimonials or any patient submission (`lib/auth/policy.ts:79-95`, `:127-133`). They can read the staff list (`policy.ts:131`, `app/(admin)/admin/(dashboard)/users/page.tsx:34`). All content deletes are soft (`policy.ts:36-38`) | Content: **A** as the spec says, editors delete content. **B** as the code does, sensitive content is admin-only delete. **C** editors may soft-delete content; patient data stays admin-only.<br>Staff list: **A′** admin only, as the spec says. **B′** editors get read-only access. | **C and A′.** An editor can already take a page off the site by unpublishing it, so blocking a reversible delete protects little. Patient-data deletes stay admin-only; amend §8 to say so. The staff list shows emails and lock state, and no editor workflow needs it; the booking assignment dropdown can use its own query. | **C** and **A′** — accepted (2026-09-15). |
| **B3 What contributors can do** | §8 Roles: "Create and edit their own content only. Publish requires an editor or admin" | "Media personnel" can read, create and update any row in `blog_posts`, `authors`, `faqs` and `awards`, and can upload or delete any media file. They get nothing on services, doctors, locations, HMOs or testimonials (`lib/auth/policy.ts:97-104`, `:135-141`). There is no ownership check: `can()` sees only role, action and resource (`policy.ts:116`), and nothing writes `created_by_user_id` (`lib/db/schema.ts:145`) | **A** As the spec says: own rows only, on every content type. Needs `created_by_user_id` written on insert and a row-aware check alongside `can()`.<br>**B** As the code does: an allow-list of resources, no ownership.<br>**C** Both: keep the allow-list and add "own rows only" for updates. | **C**, if "media personnel" is a real Paelon role; otherwise **A**. The column was added for exactly this (`docs/decisions.md:35`). Ownership of media files needs a media table; see D3. Amend §8. | **C** — taken on the assumption that media personnel is a real Paelon role; revisit as **A** if not (2026-09-15). |
| **B4 Audit log** | Not in §8 or §14 | `audit_log` (`lib/db/schema.ts:526-550`) was added to answer "who viewed or changed this booking" (`docs/decisions.md:34`). It is written for sign-ins, staff changes and media deletes (`lib/auth/config.ts:117-163`, `lib/auth/user-actions.ts:144-305`, `lib/uploadthing/actions.ts:85-90`). Reads are not recorded. `ip_address` is never filled in (`lib/db/queries/users.ts:116-143`). Only `admin` can read it, and there is no viewer (`policy.ts:119-121`). Retention is undefined | Views: log changes only, or also opens of a patient record's detail page.<br>IP: none, a salted hash (as the rate limiter does), or raw.<br>Retention: indefinite, or a fixed period. | **Add it to §8.** Log detail-page opens of bookings, contact and corporate records (not list views). Store a salted IP hash. Counsel sets the retention period together with E5. Build a read-only `/admin/audit` for `admin`. | Accepted as recommended; retention period with counsel (2026-09-15). |

---

## C. Admin surfaces

| Topic | Spec says (§ ref) | Code does (file:line) | Options | Recommendation | Decision |
|---|---|---|---|---|---|
| **C1 Route map and dashboard** | §8 Admin routes: 16 routes, one CRUD route per content type, plus `/admin/contact`, `/admin/corporate-enquiries`, `/admin/analytics`, `/admin/settings`. The dashboard shows "recent bookings, recent contact submissions, quick stats" | Three routes exist: `/admin` (a "what you can do" list, `app/(admin)/admin/(dashboard)/page.tsx:24-32`), `/admin/media` (not in the spec) and `/admin/users`. The nav comment proposes a different grouping: `/admin/content`, `/admin/enquiries`, `/admin/audit` (`(dashboard)/layout.tsx:29-33`) | **A** The spec's per-type routes.<br>**B** The grouped routes from the nav comment. | **A.** Add `/admin/media` and `/admin/audit` to §8. Build the dashboard as the spec describes; the capabilities list can sit below it. | **A** — accepted, `/admin/media` and `/admin/audit` added (2026-09-15). |
| **C2 `/admin/settings` scope** | §8 routes: "Site settings, feature flags". §8 Roles: `admin` owns "settings" | Nothing is built and there is no settings table. What exists today lives in env or code: `RESEND_ENABLED` (`lib/env.ts:65`), the `CSP_ENFORCE` constant (`middleware.ts:19`), `CONSENT_TEXT_VERSION` (`lib/env.ts:85`), `NEXT_PUBLIC_WHATSAPP_NUMBER` (`:108`), and `NEXT_PUBLIC_EMERGENCY_LINE` (`:109-112`). The error boundaries read the emergency line raw and cannot query the DB. Branch inboxes are already a column (`locations.booking_email`). The response-time promise is still `TODO(seed)` | **A** Drop the route. Settings stay in env and seed, and changes ship by deploy.<br>**B** A narrow `site_settings` table for non-secret display values only (response-time promise, public WhatsApp number). Integration toggles, secrets and the emergency line stay in env.<br>**C** Runtime feature flags that include integration toggles. Secrets still need env, and every flag read on a hot path costs a query or a cache entry. | **A** within Phase 2's one-week budget. Use **B** only if Paelon names a specific value they need to change without a deploy. Either remove the route from §8 or list its fields there. | **A** — route removed from §8 (2026-09-15). |
| **C3 Admin editing of legal pages** | §8 CRUD list has no legal route. §6 Privacy/Terms: "MDX content". §3: `content/ # MDX for legal pages if any` | `content/legal/{terms,privacy}.json` is imported at build (`lib/legal.ts:1-2`, `:46`) and validated by `lib/validation/legal.ts`. The files are JSON, not MDX. Admin editing was declined in Phase 1 (`docs/decisions.md:59`, `content/legal/README.md:45-51`). `CONSENT_TEXT_VERSION` is an env var (`lib/env.ts:85`) stamped on every consenting row | **A** Keep the pages in git. Changes go through a PR and counsel, git history is the version record, and the same deploy bumps `CONSENT_TEXT_VERSION`.<br>**B** `/admin/legal` plus a `legal_pages` table with immutable published versions, and the consent version taken from the published row instead of env.<br>**C** Admins edit drafts; publishing still happens by deploy. | **A**, with §3/§6 amended to say JSON. Legal text changes rarely and must pass counsel. Each consent row points at a wording version, and a page edited in place breaks the link between that version and the words the person agreed to. Choose **B**, with versioning, only if Paelon insists. | **A** — accepted (2026-09-15). |
| **C4 Draft preview and caching** | §8 Content editing: a "Preview" button "opens the marketing site in draft mode showing the unpublished state". §5 Query patterns: "Never cache authenticated queries" | Public reads go through `cachedRead` (a wrapper around `unstable_cache`) with cache tags, and filter to published rows (`lib/db/queries/shared.ts:36-56`). `draftMode` is used nowhere | **A** Next's `draftMode()`, turned on by an admin-gated route. In draft mode the getters skip the cache and the published filter.<br>**B** A separate `/admin/preview/[type]/[slug]` that renders the marketing template inside the admin panel. | **A.** It reuses the real templates. The route that enables it must call `requireCan("read", resource)`, and draft reads must bypass `cachedRead`, as §5 requires. | **A** — accepted (2026-09-15). |
| **C5 Booking workflow rules** | §8 Booking workflow: `new → contacted → confirmed → completed`, with `cancelled` and `no_show` drawn branching from `contacted` only. Internal notes are "rich text". Bookings are assigned to editors or admins | The enum has all six states (`lib/db/schema.ts:59-66`), but no transition rules exist. `internal_notes` is plain `text` (`:596`). There is `assigned_to_user_id` (`:593`) and a history table (`:633-652`). Editors can read bookings; contributors cannot (`policy.ts:127-141`). Nothing is built | Open questions: may `new` go straight to `cancelled`? May `confirmed` become `no_show` or `cancelled` (the diagram says no, but a no-show happens after confirmation)? Are backward moves allowed? Are notes rich text or plain? Who may read `reason_for_visit` (`docs/decisions.md:23` says "staff")? | **Transitions:** `new→contacted/cancelled`, `contacted→confirmed/cancelled/no_show`, `confirmed→completed/cancelled/no_show`. Backward moves allowed for `admin` only, with a required note. Keep them in one transition map in `lib/booking/` and redraw the §8 diagram.<br>**Notes:** plain text.<br>**`reason_for_visit`:** editors and admins only, with each view logged (B4). | Transitions, plain-text notes and `reason_for_visit` access as recommended (2026-09-15). |

---

## D. Content editing

| Topic | Spec says (§ ref) | Code does (file:line) | Options | Recommendation | Decision |
|---|---|---|---|---|---|
| **D1 Rich-text editor library** | §8 Content editing: "Rich text where sensible (bio, description, blog body) via a lightweight editor. Recommendation: Lexical or Tiptap. Confirm before installing" | Nothing is installed. Long text is parsed into a typed AST (`lib/markdown.ts`) and rendered as React elements (`components/site/article-body.tsx`), with no `dangerouslySetInnerHTML` | **A** Tiptap: WYSIWYG, but it stores JSON or HTML. It needs Markdown serialisation to match the stored format, which means another package; HTML output would bypass the safe renderer.<br>**B** Lexical: the same trade-off, via `@lexical/markdown`.<br>**C** No library: a Markdown textarea with a syntax-inserting toolbar and a live preview rendered by `lib/markdown.ts`. | **C.** No new dependencies, one renderer for both the preview and the live site, and the stored format does not change. Revisit Tiptap if editors struggle with Markdown after a trial. The admin bundle is outside the marketing budgets, so size is not the concern; the format is. | **C** — accepted (2026-09-15). |
| **D2 Blog body format** | §5 blog_posts: `body text (MDX)`. §8: "MDX for blog posts (with a preview pane)" | A Markdown subset, parsed by `lib/markdown.ts`. No MDX compiler is on §2. The stored source is still valid MDX (CLAUDE.md, live gaps) | **A** Ratify Markdown and amend §5/§8; the preview pane uses the same renderer (D1).<br>**B** Add an MDX compiler. It evaluates code from database content and needs approval. | **A.** Editors are not going to write JSX, and running code from a CMS field is a risk with no benefit here. | **A** — accepted (2026-09-15). |
| **D3 Alt text for uploaded images** | §12 Screen readers / CLAUDE.md baseline 10: meaningful alt text on every image; a missing one is a blocking failure | Image columns hold only a URL or key, and the schema has no alt column anywhere. The 11 image columns are listed at `lib/db/queries/media-references.ts:39-54` (its comment says thirteen). Media references are found by substring search across 9 tables (`:62-96`). The media grid uses the filename as alt text, next to the visible filename (`components/admin/media-grid.tsx:152-156`) | **A** One `*_alt` column per image column. That is a migration touching 11 columns, and the two array columns need parallel arrays or `jsonb`.<br>**B** A `media` table (key, url, alt, uploaded_by, created_at) referenced by foreign key.<br>**C** Derive alt text from the row (doctor name, post title). This works for headshots and logos, not for hero or gallery images. | **B.** One migration fixes alt text, contributor ownership of media (B3) and reference tracking, which replaces the substring scan. C is an acceptable fallback for headshots and logos. In the grid, set the thumbnail to `alt=""` because the filename is already next to it. | **B** — accepted, with C for headshots and logos (2026-09-15). |

---

## E. Integrations and operations

| Topic | Spec says (§ ref) | Code does (file:line) | Options | Recommendation | Decision |
|---|---|---|---|---|---|
| **E1 UploadThing size limit and WebP transcode** | §10 UploadThing: "Types allowed: jpeg, png, webp. Max size: 5 MB per image (transcode larger uploads server-side to WebP)" | Route key `image` with `maxFileSize: "4MB"` and `maxFileCount: 10` (`lib/uploadthing/core.ts:40-47`); the comment above it says 5 MB (`:39`). The `image` key accepts any image MIME type. JPEG, PNG and WebP are enforced only by the client's `accept` attribute (`components/admin/media-uploader.tsx:85`), and the UI says 4 MB (`:77`). There is no transcoding. Files go straight from the browser to UploadThing, so the server never holds the bytes | **A** Spec as written, transcoding on the server. Either the bytes pass through our function, where Vercel's ~4.5 MB request-body cap rules out the >5 MB case, or `onUploadComplete` fetches the file, runs sharp, re-uploads and deletes the original. That makes sharp a runtime dependency (today it is dev-only) and stores two objects per upload.<br>**B** Downscale and re-encode to WebP in the browser (canvas, no dependency) before uploading, then enforce 5 MB.<br>**C** Reject files over 5 MB with a clear message and no transcoding.<br>In every case, replace `image` with the `"image/jpeg"`, `"image/png"` and `"image/webp"` keys so the server enforces the types; SVG on a public CDN can carry script. | **C plus the typed keys now**, with §10 amended. Switch to **B** if editors regularly hit the cap with phone photos. | **C** plus typed route keys — accepted (2026-09-15). |
| **E2 Umami hosting** | §9 Deployment: "separate Vercel project pointing at the same Postgres database (… separate namespace or DB)", or the same project as a route group; "default to a separate deployment". §2: self-hosted | `NEXT_PUBLIC_UMAMI_*` env names (`lib/env.ts:106-107`; the prefix correction is at `docs/decisions.md:65-68`). The CSP adds the script origin to `script-src` and `connect-src` (`middleware.ts:43-52`). `drizzle.config.ts` has no schema or table filter. **Phase 1 gap, not a Phase 2 choice:** there is no tracking script in `app/layout.tsx`, no `lib/analytics/` (§3), and none of the §9 custom events | **A** A separate Vercel project with a separate Neon database.<br>**B** A separate project sharing Paelon's database in its own schema. Safe only if Umami is kept out of `public`: drizzle-kit diffs `public`, and Umami's tables there would look like drift.<br>**C** Umami Cloud. Less to operate, but it is not self-hosted (§2) and the analytics data leaves Paelon's infrastructure.<br>**D** A route group. Not practical: Umami is its own Next.js app. | **A.** It isolates Umami from the app's migrations, gives a clean backup and restore boundary, and moves to the self-hosted box post-launch without untangling schemas. Raise the missing tracking script as a Phase 1 item separately. | **A** — accepted (2026-09-15). |
| **E3 Umami dashboard in the admin panel** | §9 Dashboard integration: an iframe in `/admin/analytics`; "Grant admin users access via Umami's share URLs or SSO" | Nothing is built. `frame-src 'none'`, with a comment saying `/admin/analytics` will need the Umami origin (`middleware.ts:53-55`) | **A** A share-URL iframe. The URL is a bearer link: anyone who has it can see the stats, which contain no PII under §9.<br>**B** A separate Umami login for each staff member.<br>**C** SSO. Umami has no built-in link to Auth.js, so this would be custom work in front of it. | **A.** Allow framing only on `/admin/analytics` (E4), limited to `admin` as §9 says. Confirm at build time that Umami's share page allows itself to be framed. | **A** — accepted (2026-09-15). |
| **E4 Content Security Policy for admin pages** | §14 Headers: strict CSP, `X-Frame-Options: DENY` | One policy for every route, in report-only mode (`middleware.ts:19`). `connect-src 'self'` has no UploadThing ingest origin (`:52`), so browser uploads will fail once the policy is enforced. `frame-src 'none'` (`:55`) blocks E3 | **A** A separate CSP for `/admin/*` that adds the UploadThing ingest and Umami origins, while marketing pages keep the tight one.<br>**B** Widen the single global policy. | **A.** Take the exact ingest hosts from UploadThing's docs for the app's region instead of guessing them. | **A** — accepted (2026-09-15). |
| **E5 Retention job** | §14 NDPR: "booking submissions retained for 12 months after appointment date, then archived". "No automated deletion in Phase 1". Not in §1's Phase 2 list | Anonymise-in-place was chosen (`docs/decisions.md:39`). `anonymised_at` exists on bookings, contact and corporate (`lib/db/schema.ts:604`, `:672`, `:697`), but not on `newsletter_subscribers` (`:711-743`). `JOBS_SECRET` is declared (`lib/env.ts:83`) and `.env.example:18` mentions `/api/jobs/*`, but no such route exists, and there is no scheduler (no `vercel.json`). Bookings have only `preferred_date`, not an appointment date (`:574`). Personal data also sits in `internal_notes` (`:596`), `booking_status_history.note` (`:649`) and `audit_log.metadata` | Wording: "archived" (spec) or "anonymised" (code).<br>Retention periods for contact, corporate and newsletter: the spec is silent.<br>Start date: `preferred_date`, or a confirmed date that does not exist yet (C5).<br>Scheduler: **A** a `POST /api/jobs/retention` endpoint guarded by `JOBS_SECRET`, run by Vercel Cron now and system cron after self-hosting. **B** a scheduled GitHub Actions workflow calling the same endpoint. **C** a schedule inside the database, which ties the logic to Neon. | **Amend §14 to "anonymised in place"** and count 12 months from `preferred_date`. Counsel sets the contact, corporate and newsletter periods. Build scheduler **A**: idempotent, and it also nulls the notes fields. It is not urgent within Phase 2, but it must ship before the first booking reaches 12 months past its `preferred_date`. | Accepted as recommended, scheduler **A**; periods with counsel (2026-09-15). |

---

## F. Quality gates

| Topic | Spec says (§ ref) | Code does (file:line) | Options | Recommendation | Decision |
|---|---|---|---|---|---|
| **F1 Do a11y and performance gates apply to the admin panel?** | §12 accessibility requirements and §13 performance budgets don't say whether they cover `/admin`. CLAUDE.md: "WCAG 2.1 AA on every template" | The admin components have labels, `aria-live` and text-not-colour states. The media grid asserts "the admin panel is not on any performance budget" and uses a plain `<img>` (`components/admin/media-grid.tsx:145-156`). There are no admin a11y tests | **A** WCAG AA and §13 budgets both apply.<br>**B** WCAG AA applies; the budgets cover marketing only.<br>**C** Neither applies. | **B**, stated in §12/§13. Staff are users too. Add the admin pages to the axe and Playwright runs when the Phase 2 E2E suite lands. | **B** — accepted (2026-09-15). |

---

## Found while reading — not decisions

Fix these when the area is next touched:

- `lib/uploadthing/core.ts:73-75` reads `contentType` and `slug` from the query
  string, but `components/admin/media-uploader.tsx` never sends them, so every
  upload logs `"unknown"`.
- `app/api/uploadthing/route.ts:9-11` still says the route is refused "unless
  uploads are explicitly enabled", which describes the dead env var.
- `app/(admin)/admin/login/page.tsx:15` points to a script that does not exist (A6).
- `lib/db/queries/users.ts:42` says the role is re-checked "on every request"; it
  is every 5 minutes (A3).
- `lib/db/queries/media-references.ts:22` says thirteen columns; it lists eleven.
- `lib/db/schema.ts:464` says the argon2 library is not approved, while the
  library is installed and in use (A7).
